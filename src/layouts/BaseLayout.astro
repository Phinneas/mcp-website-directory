---
/**
 * BaseLayout.astro — shared shell for every page.
 *
 * Props:
 *   title        — <title> tag (required)
 *   description  — meta description
 *   canonical    — canonical URL (defaults to current page)
 *   ogImage      — Open Graph image URL
 *   structuredData — JSON-LD object (optional)
 */
import { ViewTransitions } from 'astro:transitions';
import Navbar from '../components/Navbar.astro';
import EmailSignup from '../components/EmailSignup.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  canonical?: string;
  ogImage?: string;
  structuredData?: object;
  bodyClass?: string;
}

const {
  title,
  description = 'Your curated collection of Model Context Protocol servers for enhanced AI capabilities',
  canonical = Astro.url.href,
  ogImage = 'https://www.mymcpshelf.com/og-image.jpg',
  structuredData,
  bodyClass = '',
} = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Dark mode flash prevention — must run before any styles -->
  <script is:inline>
    (function() {
      if (localStorage.getItem('theme') === 'dark') {
        document.documentElement.classList.add('dark-mode');
      }
    })();
  </script>

  <title>{title}</title>
  <meta name="description" content={description}>
  <link rel="canonical" href={canonical}>

  <!-- Verification -->
  <meta name="google-site-verification" content="qmL-eYN3ygqiuLXVWHuWWko8vIBJj3hAYizdpo7dDug">
  <meta name="msvalidate.01" content="8AA5A1EBF656D16B95B78BE654843905">

  <!-- Favicons -->
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link rel="icon" href="/favicon.png" type="image/png">

  <!-- Open Graph -->
  <meta property="og:type" content="website">
  <meta property="og:url" content={canonical}>
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  <meta property="og:image" content={ogImage}>

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:url" content={canonical}>
  <meta name="twitter:title" content={title}>
  <meta name="twitter:description" content={description}>
  <meta name="twitter:image" content={ogImage}>

  <!-- Structured Data -->
  {structuredData && (
    <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  )}

  <!-- Feeds -->
  <link rel="alternate" type="application/rss+xml" title="My MCP Shelf — Verified &amp; Security Feed" href="https://www.mymcpshelf.com/api/v1/feed/rss.xml" />
  <link rel="alternate" type="application/json" title="My MCP Shelf — JSON Feed" href="https://www.mymcpshelf.com/api/v1/feed/feed.json" />

  <!-- Analytics -->
  <script defer data-domain="mymcpshelf.com" src="https://plausible-analytics-ce-production-7165.up.railway.app/js/script.js"></script>

  <!-- Smooth page transitions — eliminates flash on navigation -->
  <ViewTransitions />

  <!-- Per-page extra head content (additional styles, etc.) -->
  <slot name="head" />
</head>
<body class={bodyClass}>
  <Navbar />
  <slot />
  <EmailSignup />
  <footer class="site-footer">
    <div class="footer-inner">
      <div class="footer-links">
        <a href="/changelog">Changelog</a>
        <a href="/api/v1/feed/rss.xml">RSS</a>
        <a href="/api/v1/feed/feed.json">JSON Feed</a>
        <a href="/llms.txt">llms.txt</a>
        <a href="/audit-manifesto">Audit Methodology</a>
      </div>
      <p class="footer-copy">© {new Date().getFullYear()} My MCP Shelf · Curated, verified, execution-tested.</p>
    </div>
  </footer>
</body>
</html>

<style is:global>
  /* ── Base reset & typography ───────────────────────────── */
  *, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: 'Libre Franklin', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    line-height: 1.7;
    color: #f1f5f9;
    background: #0f172a;
    min-height: 100vh;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: 'Space Grotesk', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }

  /* ── View transition animations ───────────────────────── */
  @keyframes fade-in {
    from { opacity: 0; transform: translateY(6px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  @keyframes fade-out {
    from { opacity: 1; transform: translateY(0); }
    to   { opacity: 0; transform: translateY(-6px); }
  }

  ::view-transition-old(main-content) {
    animation: 150ms ease-in fade-out;
  }

  ::view-transition-new(main-content) {
    animation: 200ms ease-out fade-in;
  }

  /* ── Dark mode ─────────────────────────────────────────── */
  html.dark-mode body {
    background: #0f172a;
    color: #f1f5f9;
  }

  /* ── Site footer ───────────────────────────────────────── */
  .site-footer {
    background: #0f172a;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    padding: 2rem 1.5rem;
    text-align: center;
  }

  .footer-inner {
    max-width: 800px;
    margin: 0 auto;
  }

  .footer-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem 1.25rem;
    justify-content: center;
    margin-bottom: 1rem;
  }

  .footer-links a {
    font-size: 0.85rem;
    color: #94a3b8;
    text-decoration: none;
    transition: color 0.15s ease;
  }

  .footer-links a:hover {
    color: #f59e0b;
  }

  .footer-copy {
    font-size: 0.78rem;
    color: #64748b;
  }
</style>
