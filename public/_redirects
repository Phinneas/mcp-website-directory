# Cloudflare Pages Redirects for MCP Directory
# Maintains SEO while transitioning to deployment-based navigation

# Homepage - no redirect (serves index.html)
# / /local-stdio 301

# Preserve old URLs (no redirects needed for these - they remain valid)
# /mcp-clients - stays as is
# /mcp-tools - stays as is
# /blog/* - stays as is
# /faq - stays as is
# /submit-server - stays as is
# /server/* - stays as is (individual server pages)

# Old category-style URLs (if they existed - no known URLs to redirect)

# Common misspellings and variants
/enterprise /enterprise-saas 301
/cloud /cloud-native 301
/local /local-stdio 301
/vpc /self-hosted 301
/selfhosted /self-hosted 301
/cliservers /local-stdio 301

# Legacy paths that might be bookmarked
/home /local-stdio 301
/main /local-stdio 301
/index /local-stdio 301

# External shorteners and social media (common typos)
/stdio /local-stdio 301
/sse /cloud-native 301
/websocket /cloud-native 301

# Ensure all deployment pages have trailing slash variants
/local-stdio/ /local-stdio 301
/cloud-native/ /cloud-native 301
/self-hosted/ /self-hosted 301
/enterprise-saas/ /enterprise-saas 301
/enterprise-readiness/ /enterprise-readiness 301

# Blog post redirects for old content that might have been shared
# (No old URLs to redirect - keeping structure)

# API redirects (if any old API paths existed)

# Final catch-all for unmatched paths (send to homepage with 404 handling)
# Handled by 404.astro - no redirect needed
